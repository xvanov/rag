# Secrets + local config
.env
app.settings.json

# Scraper recon / session artifacts (cookies, login captures -- never commit)
scraper/recon/
**/cookies*.json
**/login_status.json
scraper/_ncosfm_cache/

# Indexes + uploaded corpora (regenerable / large)
.index/
corpora/

# Scout listings data instance (typed listings.db + semantic corpus/index +
# commute cache) -- local, regenerable, may hold scraped listing data. Never commit.
listings_data/

# Containerized runtime: ./data is the docker-compose bind mount holding the
# .index + corpora of a migrated instance; bundles are the migrate tarballs.
data/
docrag-bundle*.tar.gz
*.tar.gz

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
